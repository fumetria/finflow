# Dependencies (reinstalled inside the image)
node_modules
**/node_modules
.pnpm-store

# Build outputs
dist
**/dist
build
**/build
*.tsbuildinfo

# Env / secrets (injected via compose, never baked into images)
.env
.env.*
!.env.example

# VCS / tooling
.git
.gitignore
.claude
.vscode
.idea

# Logs & coverage
*.log
coverage
*.lcov

# Misc
extra
ops/grafana/data
Thumbs.db
.DS_Store
